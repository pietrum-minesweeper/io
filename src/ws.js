/**
 * Load dependencies.
 */
const dotenv = require('dotenv');

/**
 * Configuration.
 */
dotenv.config();

/**
 * Debugger.
 */
const debug = require('debug')('minesweeper:ws');

/**
 * Initialize.
 */
debug('initialize');
